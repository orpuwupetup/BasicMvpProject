<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="top_notification_background_style">
        <item name="android:background">@drawable/background_top_notification</item>
        <item name="android:elevation">5dp</item>
    </style>

    <style name="top_notification_text_style">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/pureWhite</item>
        <item name="android:layout_marginStart">@dimen/material_design_small_margin_size</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
    </style>

    <style name="close_top_notification_button_style">
        <item name="android:layout_width">24dp</item>
        <item name="android:layout_height">24dp</item>
        <item name="android:background">@drawable/background_close_top_notification_button</item>
        <item name="android:src">@drawable/ic_close_warm_grey</item>
    </style>

</resources>
